package com.plainstudio.stackcasino.ui.theme

import androidx.compose.ui.graphics.Color

// Brand accents.
val AccentViolet = Color(0xFF8B5CF6)
val AccentVioletSoft = Color(0xFFA78BFA)

// Surfaces from the mockup design tokens (dark-only product).
val SurfaceBase = Color(0xFF0B0B12)
val SurfaceRaised = Color(0xFF15141F)
val SurfaceElevated = Color(0xFF1E1C2E)
val SurfaceOutline = Color(0x14FFFFFF)

// Text ramp.
val TextHigh = Color(0xFFF5F5FA)
val TextMedium = Color(0xFFA1A1B3)
val TextLow = Color(0xFF6B6B80)

// Semantic palette. Exposed via StackcasinoColors below for the
// states (win/loss/warn/info) that Material 3's standard ColorScheme
// does not model.
val SemanticOk = Color(0xFF22C55E)
val SemanticDanger = Color(0xFFEF4444)
val SemanticWarn = Color(0xFFF59E0B)
val SemanticInfo = Color(0xFF3B82F6)
