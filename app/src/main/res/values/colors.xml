<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Single XML color in use today: the activity-level window background
         that matches Compose's SurfaceBase token, so there is no flash of
         white before the first Compose frame. The full design palette lives
         in ui/theme/Color.kt where Compose can consume it. -->
    <color name="stackcasino_window_background">#FF0B0B12</color>
</resources>
